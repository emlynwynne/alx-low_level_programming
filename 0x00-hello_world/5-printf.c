#include <studio.h>
/**
 * main -A program print a line using using the printf function
 * Return:0 (Sucess)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return(0);
}	
